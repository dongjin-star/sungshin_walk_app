# PINWALK — PRD (통합본)

산책 중 찍은 장면에 AI가 캡션·태그·무드를 붙이고, 지도·피드·대시보드로 쌓이는 개인 기록 PWA

| 항목 | 내용 |
|---|---|
| 문서 버전 | **v1.0 (통합본)** |
| 작성일 | 2026-08-26 |
| 작성자 | JJIn |
| 상태 | **디자인 착수 가능** |
| 유형 | PWA (설치형 웹앱) · 모바일 우선 |
| 기술 스택 | Next.js(App Router) + TypeScript / Supabase / Gemini Vision / Leaflet + OSM |
| 배포 | Vercel (Route Handler = 서버리스 함수) |
| 외부 연동 제약 | **Gemini Vision, Supabase 두 가지만 사용한다** |
| 서비스명 | **임시.** → 24장 열린 이슈 1번 |

---

## 0. 이 문서를 읽는 법

이 문서는 **디자인 착수를 위한 단일 소스**다. 이전에 나뉘어 있던 기획·기술 문서를 하나로 합쳤다.

| 파트 | 내용 | 주 독자 |
|---|---|---|
| **PART 1** (1~4장) | 제품 정의 · 타깃 · 우선순위 · 범위 | 전원 |
| **PART 2** (5~11장) | **화면 설계 · 상태 · 카피** | **디자인** |
| **PART 3** (12~14장) | **디자인 파운데이션 · 컴포넌트 · 무드 팔레트** | **디자인** |
| **PART 4** (15~21장) | 아키텍처 · 데이터 · AI · 보안 · 배포 | 개발 |
| **PART 5** (22~25장) | 우선순위 · 비기능 · 리스크 · 열린 이슈 | 전원 |

**디자인 작업 시 PART 2와 PART 3이 주 작업 범위다.** PART 4는 화면에 영향을 주는 제약(로딩 시간, 상태 종류, 오프라인)을 확인할 때만 참조하면 된다.

> **PART 3(디자인 파운데이션)은 확정이 아니라 제안이다.** 기존 PROJECT HUB의 라이트 에디토리얼 톤을 기준선으로 잡았다. 방향을 바꾸려면 12장부터 갈아엎으면 되고, 그 아래 화면 설계(PART 2)는 대부분 그대로 쓸 수 있게 분리해 두었다.

---

# PART 1 — 제품 정의

## 1. 개요

### 1.1 한 줄 정의

산책하다 마음에 드는 장면을 찍으면, AI가 **한 줄 캡션 · 태그 · 무드**를 붙여주고
**지도 · 피드 · 대시보드** 세 얼굴로 쌓이는 나만의 동네 탐험 기록.

### 1.2 배경 및 문제 정의

산책 중 찍은 사진은 **찍히기는 하지만 남지는 않는다.**

| 지금 벌어지는 일 | 한계 |
|---|---|
| 카메라 롤에 저장된다 | 스크린샷·영수증과 섞여 3일 뒤 사라진다 |
| 인스타에 올린다 | 남에게 보여줄 만한 것만 남는다. 골목 담장은 안 올린다 |
| 지도 앱에 저장한다 | 장소는 남지만 **그날의 장면**은 안 남는다 |
| 일기에 쓴다 | 쓰는 비용이 커서 3일이면 그만둔다 |

핵심 병목은 촬영이 아니라 **기록하는 비용**이다. "여기 좋았다"를 문장으로 옮기는 30초가 아까워서 대부분 그냥 넘어간다. AI가 그 30초를 대신 치러주면 **사진 한 장이 곧 기록 한 편**이 된다.

이 서비스가 파는 것은 "AI 캡션"이 아니라 **"셔터 한 번 = 기록 한 줄"** 이라는 비용 절감이다.

### 1.3 세 개의 얼굴

같은 데이터를 세 방식으로 자른다. **추가 촬영 없이 세 번 보상을 준다**는 게 이 구조의 핵심이다.

| 뷰 | 답하는 질문 | 심리 |
|---|---|---|
| **지도** | 내가 어디를 다녔지? | 수집 · 탐험 |
| **피드** | 그날 뭘 봤지? | 회고 · 감상 |
| **대시보드** | 나는 어떤 걸 찍는 사람이지? | 자기 발견 |

> **대시보드가 이 제품의 숨은 무기다.** 지도와 피드는 사진을 다시 보여줄 뿐이지만, 대시보드는 **사진에 없던 사실**을 알려준다 — "나는 화요일에 거의 안 찍는구나", "내 사진의 40%가 고요 무드구나". 사용자가 스스로도 몰랐던 걸 알려주는 화면이라 재방문 동기가 가장 강하다.

### 1.4 v1의 성공 정의

| 지표 | 목표 |
|---|---|
| 셔터 → 지도에 핀 표시 | **3초 이내** (캡션 대기 없이) |
| 캡션 생성 성공률 (`ready` 도달) | 95% 이상 |
| 첫 촬영 후 **7일 내 2회차 촬영** 도달률 | 40% 이상 |
| 온보딩 완주율 (마지막 장 → 로그인 진입) | 80% 이상 |
| 사용자 데이터가 다른 사용자에게 노출되는 사건 | **0건 (필수)** |
| Gemini 키가 클라이언트 번들·네트워크·Git에 노출 | **0건 (필수)** |
| 6가지 사진 상태 구현 | 6/6 |

## 2. 설계 우선순위

> 1. **지도 수집감**
> 2. **회고 (피드 · 대시보드)**
> 3. AI 캡션·무드의 품질
> 4. 촬영 속도

충돌하면 위쪽이 이긴다. 두 가지 직접적 귀결:

- **앱을 열면 지도가 뜬다.** 타임라인이 아니다.
- **캡션 품질(3위)과 촬영 속도(4위)는 양립 불가능하므로 시간축에서 분리한다.** → 9장

## 3. 타깃 사용자

### 3.1 Primary — "동네를 다시 보는 사람" (25~40세)

> 매일 지나는 길인데, 어제랑 오늘이 다르다.

출퇴근길·저녁 산책 등 **반복되는 동선**을 가진다. 찍고 싶은 순간은 자주 오는데, 찍어놓고 나면 뭘 해야 할지 모른다.
**성공** = 3주 뒤 지도를 열고 "내가 이만큼 봤구나"

### 3.2 Secondary — "이사 온 사람" (20~35세)

> 이 동네를 아직 모른다.

새 동네를 익히는 중이다. 탐험 자체가 목적이다.
**성공** = 핀이 없는 방향으로 일부러 걸어본다

### 3.3 Tertiary — "자기를 궁금해하는 사람"

> 내가 뭘 좋아하는지 나도 잘 모른다.

MBTI·리스닝 리포트·연말 결산을 즐긴다.
**성공** = 대시보드를 열어 무드 분포를 확인하고 스크린샷을 찍는다

### 3.4 사용자 스토리

| ID | 스토리 |
|---|---|
| US-01 | 처음 들어왔을 때 이 앱이 뭘 하는 앱인지 3장 안에 이해한다 |
| US-02 | 산책 중 장면을 만나면 3초 안에 찍고 다시 걷는다 |
| US-03 | 찍고 나서 아무것도 안 해도 나중에 캡션·태그·무드가 붙어 있다 |
| US-04 | 갤러리에 있던 사진도 올려서 기록에 넣는다 |
| US-05 | 지도를 열면 내가 찍은 자리들이 핀으로 깔려 있다 |
| US-06 | 핀이 비어 있는 방향을 보고 오늘 걸을 길을 정한다 |
| US-07 | 피드를 아래로 내리며 지난 산책들을 되짚는다 |
| US-08 | 내가 가장 많이 쓴 태그가 뭔지 워드클라우드로 본다 |
| US-09 | 무슨 요일에 많이 걷는지, 내 사진의 무드가 어느 쪽인지 본다 |
| US-10 | 캡션이 마음에 안 들면 고치거나 다시 만든다 |
| US-11 | 지하나 데이터 안 터지는 곳에서 찍어도 잃어버리지 않는다 |
| US-12 | 내 사진은 나만 본다. 다른 기기에서 로그인해도 그대로 있다 |

## 4. 범위

### 4.1 In Scope (v1) — 확정 피처 14개

| # | 기능 | 비고 |
|---|---|---|
| 1 | 스플래시 화면 | PWA standalone 진입 시 |
| 2 | 온보딩 3장 | **최초 1회만** |
| 3 | 로그인 | Supabase Auth |
| 4 | 카메라 촬영 | |
| 5 | 갤러리 사진 업로드 | 위치 처리 별도 (9.5) |
| 6 | GPS 위치 기록 | 촬영 시점 Geolocation |
| 7 | AI 감성 캡션 | 1행, 짧은 시 톤 |
| 8 | AI 태그 | 자유 생성 + 수렴 전략 |
| 9 | **AI 무드** | **8종 고정 분류** |
| 10 | 저장 | Supabase Storage + Postgres |
| 11 | 지도 뷰 | Leaflet + OSM |
| 12 | 산책 기록 피드 | 날짜 그룹 카드 리스트 |
| 13 | 대시보드 | 태그 워드클라우드 · 요일별 그래프 · 무드 분포 |
| 14 | PWA | manifest + SW + 오프라인 큐 |

### 4.2 Out of Scope (v1)

- 공유·팔로우·피드 공개 — **개인 기록이다.** 공유가 들어오면 집 위치 노출 문제가 통째로 따라온다
- 산책 세션·경로 추적 — "사진 낱개" 확정. 백그라운드 위치 추적은 PWA에서 사실상 불가능
- **날씨 정보 · 동네 이름 자동 인식** — 외부 연동 제약 → 4.3
- 사진 편집·필터 / 푸시 알림 / 다국어 / 연말 결산

### 4.3 외부 연동 제약이 바꾼 것

> **원칙: Gemini Vision과 Supabase 외의 외부 서비스에 의존하지 않는다.**

| 항목 | 결정 | 디자인 영향 |
|---|---|---|
| 날씨 | **제거** | 상세 화면 메타 영역에서 날씨 아이콘·기온이 빠진다 |
| 동네 이름(역지오코딩) | **제거** | 피드 그룹이 "동네별"이 아니라 **"날짜별"**. 지도·상세에 지명 텍스트가 없다 |
| 지도 | **Leaflet + OSM** | OSM 타일은 카카오/네이버보다 **한글 지명이 성기다.** 지도는 배경이고 핀이 주인공이라는 전제가 더 중요해진다 |
| 캡션 파이프라인 | 3단계 → **1단계** | 캡션 지연 p90 10초 → **5초 이내** |

---

# PART 2 — 화면 설계

## 5. 정보 구조 (IA)

```
[스플래시]  ─▶ 온보딩 3장 (최초 1회) ─▶ 로그인 ─┐
                                                │
                        (재방문 · 로그인 상태)    │
                                                ▼
┌──────────────────────────────────────────────────────┐
│                      메인 (탭 3개)                     │
│                                                      │
│   /            지도      ← 기본 진입                  │
│   /feed        피드                                   │
│   /dashboard   대시보드                                │
│                                                      │
│              [ ◉ 촬영 ]  ← 탭바 중앙 FAB              │
└──────────────────────────────────────────────────────┘
        │
        ├─ /capture       촬영 · 갤러리 선택
        │    └─ 저장 시트 (한마디 입력 · 선택)
        │
        ├─ /photo/:id     사진 상세
        └─ /settings      설정
```

**탭 3개 + 중앙 FAB** 구조.

- 지도·피드·대시보드는 **같은 데이터의 세 단면**이라 상하 관계가 없다. 탭이 맞다.
- 촬영은 어느 탭에 있든 즉시 가능해야 하므로 **탭바 중앙 FAB**로 항상 노출.
- 기본 진입은 **지도**.

## 6. 화면 인벤토리 (디자인 산출물 목록)

| # | 화면 | 필요한 시안 |
|---|---|---|
| 1 | 스플래시 | 1 |
| 2 | 온보딩 | 3 (1·2·3장) |
| 3 | 로그인 | 1 |
| 4 | **지도** | **4** (빈 상태 / 핀 소수 / 정상 / 필터 열림) |
| 5 | 촬영 | 2 (카메라 / 저장 시트) |
| 6 | **피드** | **2** (빈 상태 / 정상) |
| 7 | **대시보드** | **2** (잠금 / 해제) |
| 8 | 사진 상세 | 2 (ready / pending) |
| 9 | 설정 | 1 |
| 10 | 공통 컴포넌트 | 탭바+FAB · 배너 3종 · 필터 칩바 · 토스트 |

**총 19시안.** 굵게 표시한 셋(지도·피드·대시보드)이 제품의 얼굴이므로 여기에 시간을 몰아준다.

## 7. 스플래시 · 온보딩

### 7.1 스플래시

| 항목 | 사양 |
|---|---|
| 노출 조건 | PWA standalone 실행 시. 브라우저 탭 접속 시에는 생략 |
| 구성 | 배경색 1도 + 로고/워드마크 중앙 |
| 최소 노출 | **인위적 지연을 넣지 않는다.** 앱 셸이 준비되면 즉시 전환 |
| 최대 노출 | 2초. 초과 시 하단에 얇은 로딩 인디케이터 |

> 스플래시는 **연출이 아니라 로딩을 가리는 장치**다. "브랜드를 보여주려고" 1.5초를 강제로 붙잡으면, 하루 3번 여는 앱에서 그건 곧 짜증이 된다.

### 7.2 온보딩 3장

**최초 1회만.** 로그인 **전**에 보여준다 — 가입할 이유를 먼저 설명해야 한다.

| 장 | 헤드라인 | 서브 | 시각 |
|---|---|---|---|
| **1** | 찍기만 하세요.<br>문장은 AI가 씁니다 | 산책하다 마음에 든 장면을 찍으면 한 줄이 붙습니다 | 사진 → 캡션이 타이핑되듯 붙는 애니메이션 |
| **2** | 찍은 자리마다<br>핀이 쌓입니다 | 걸어온 길이 지도 위에 기록으로 남습니다 | 빈 지도에 사진 핀 4개가 순차로 꽂힘 |
| **3** | 내가 어떤 걸 찍는<br>사람인지 알게 됩니다 | 태그와 무드가 모여 나를 보여줍니다 | 무드 도넛 + 워드클라우드 미리보기 |

**동작 요구사항**

- 좌우 스와이프 + 하단 인디케이터(3점)
- **[건너뛰기]를 1장부터 항상 노출.** 온보딩은 관문이 아니다
- 마지막 장 CTA: `시작하기` → 로그인
- 완료·건너뛰기 시 `localStorage.onboarded = true`, 로그인 후 `profiles.onboarded_at` 에도 기록
- **3장을 넘기지 않는다.** 4장부터 완주율이 급격히 떨어진다
- 설정에 "온보딩 다시 보기"

> **온보딩 3장은 이 앱의 유일한 설득 기회다.** 이 앱은 첫 사진을 찍기 전까지 완전히 비어 있어서 자기 자신을 설명하지 못한다. 3장 각각을 "기능 설명"이 아니라 **"내가 얻게 될 결과물"** 로 쓴다.

### 7.3 로그인

Supabase Auth. 이메일 매직링크 + 소셜 1종. 온보딩을 이미 봤으므로 설명은 짧게. 서비스명 + 한 줄 + 버튼 2개.

## 8. 지도 (`/`, 기본 탭)

**이 앱의 얼굴이다.**

### 8.1 구성

| 요소 | 사양 |
|---|---|
| 지도 | Leaflet + OSM. 초기 중심 = 마지막 핀, 없으면 현재 위치 |
| **핀** | **점이 아니라 원형 사진 썸네일.** 테두리는 **무드 색** (→ 14장) |
| 클러스터 | 밀집 시 묶고 **숫자 표시** |
| 누적 카운터 | `핀 47 · 12일` — 상단 또는 하단 고정 |
| 하단 시트 | 현재 화면 영역 내 핀들의 가로 스크롤 썸네일 (드래그로 확장) |
| 필터 칩바 | 상단. 태그 칩 + 무드 8색 점 |
| 현재 위치 버튼 | 우측, FAB 위 |

**핀 디자인 요구**

- 원형, 사진이 꽉 차게(cover). 지름 44~48px 권장
- 테두리 2px = 무드 색. `pending` 은 무채색 테두리 + 우하단 작은 점
- **선택된 핀은 확대 + 캡션 말풍선.** 말풍선 안은 캡션 1행만
- 그림자는 지도 위에서 핀을 띄우는 용도로만 1회 허용

### 8.2 빈 상태 (핀 0개) — **v1 최대 리스크 지점**

- 지도는 현재 위치 중심으로 **정상 표시한다.** 회색 플레이스홀더 금지
- 중앙 하단에 한 문장 + FAB를 가리키는 화살표
  > "마음에 드는 장면을 찍어 첫 핀을 꽂아보세요"
- **샘플 핀을 만들지 않는다.** 남의 기록이 내 지도에 있으면 안 된다

### 8.3 핀 1~3개 구간 — 별도 설계 필요

> 핀 3개짜리 지도는 "탐험 기록"이 아니라 **점 세 개**로 보인다. 수집감이 1순위인데 이 구간에서 무너지면 2회차 촬영이 안 일어난다.

- 이 구간에서는 **누적 카운터를 더 크게** 노출해 *쌓이는 중*임을 보여준다
- 하단 시트를 기본 확장 상태로 열어 사진을 크게 보여준다
- 지도 줌을 핀들이 화면을 채우도록 자동 조정한다 (핀 3개가 화면 구석에 몰려 있으면 안 된다)

## 9. 촬영 (`/capture`)

### 9.1 시간축 분리 (핵심 설계 결정)

```
[0.0s] 셔터
[0.3s] Geolocation 좌표 확보 (촬영과 병렬)
[0.8s] 로컬 리사이즈 (thumb · view 생성)
[1.2s] ┌─ 저장 시트 ───────────────────┐
       │  사진 미리보기                 │
       │  ┌─────────────────────────┐  │
       │  │ 한마디 (선택)…           │  │ ← 비워두고 넘어가도 됨
       │  └─────────────────────────┘  │
       │           [ 저장 ]             │
       └───────────────────────────────┘
[?]    저장 → 지도 복귀, 핀 즉시 표시 (status: pending)
       ────────── 여기서 사용자는 다시 걷는다 ──────────
[+2s]  업로드 완료 → /api/caption 자동 호출
[+5s]  캡션·태그·무드 도착 → 핀이 조용히 ready 로 전환
```

- **최소 경로**: 셔터 → 저장 → 끝. 약 **2초**
- **한마디를 쓰는 경로**: 몇 초 더. 그만큼 캡션이 좋아진다
- **선택은 사용자에게 있다.** 앱이 강요하지 않는다

### 9.2 저장 시트 디자인 요구

- 사진 미리보기가 시트의 절반 이상을 차지한다
- 한마디 입력란은 **placeholder로 선택임을 명시**: `한마디 (선택)`
- `저장` 버튼은 **입력 없이도 항상 활성**
- 시트를 아래로 내리면 저장 없이 취소 — **확인 다이얼로그를 띄운다** (사진 유실 방지)

### 9.3 사진 상태 머신 (디자인 필수 참조)

| 상태 | 지도 핀 | 피드 카드 / 상세 |
|---|---|---|
| `queued_offline` | 점선 테두리 | "연결되면 올라갑니다" |
| `pending` | 무채색 테두리 + 작은 점 | 캡션 자리에 **얇은 스켈레톤 1행** |
| `generating` | 동일 | 동일 |
| `ready` | 무드 색 테두리 | 캡션 · 태그 · 무드 전체 |
| `failed` | 무채색 테두리 | "캡션을 못 만들었어요" + [다시 시도] |
| `no_location` | **지도에 안 뜸** | 정상 표시 + [위치 지정] |

> **`pending` 의 디자인이 이 앱의 성패를 가른다.** 스피너를 돌리면 "아직 안 됐다"가 강조되고, 아무것도 안 그리면 사진이 초라해 보인다. **정답은 캡션 높이만큼의 얇은 스켈레톤 바 1행** — 자리는 잡되 기다림을 재촉하지 않는다.
>
> **스피너·프로그레스바·퍼센트 표시 전면 금지.**

### 9.4 위치 획득 — EXIF에 의존하지 않는다

> 웹에서 `<input type="file" capture>` 로 촬영한 사진에는 **EXIF GPS가 대부분 없다.** iOS Safari는 특히 그렇다.

**위치는 사진에서 추출하지 않는다.** 촬영 순간 `navigator.geolocation` 으로 별도 획득해 앱이 묶는다.

| 항목 | 값 |
|---|---|
| 옵션 | `enableHighAccuracy: true`, `timeout: 8000`, `maximumAge: 30000` |
| 호출 시점 | **카메라 화면 진입 시 선제 호출.** 셔터 후에 받으면 그만큼 늦어진다 |
| 권한 요청 시점 | 앱 첫 실행이 아니라 **첫 촬영 시.** 이유를 한 줄 설명한 뒤 요청 |
| 권한 거부 | 사진은 정상 저장, `no_location`. **촬영을 막지 않는다** |
| 정확도 > 100m | 저장하되 핀에 낮은 정확도 표시 |

> **위치는 이 앱의 심장이지만, 위치 때문에 촬영을 막지는 않는다.** 지하상가에서 찍은 사진을 잃는 것보다 위치 없는 사진을 갖는 게 낫다.

### 9.5 갤러리 업로드

**갤러리 사진에는 "지금 위치"가 없다.**

| 항목 | 처리 |
|---|---|
| 위치 | **자동 적용하지 않는다.** "여기서 찍은 사진인가요?" 확인 후 적용 |
| 거부 시 | `no_location` 저장. 지도엔 안 뜨고 피드·대시보드엔 뜬다 |
| 촬영 시각 | 파일 `lastModified` 우선, 없으면 업로드 시각 |
| 다중 선택 | **v1은 1장씩만.** 다중은 캡션 큐가 폭주한다 → 24장 열린 이슈 6번 |

## 10. 피드 (`/feed`)

산책 기록을 시간순으로 되짚는 화면.

| 요소 | 사양 |
|---|---|
| 정렬 | `captured_at` **내림차순** |
| 그룹 | **날짜별 스티키 헤더** (`8월 26일 화요일`) |
| 카드 | 사진(`thumb`, 4:3) · 캡션 · 태그 칩 · 무드 배지 · 시각 |
| 무드 표현 | 카드 좌측 세로 라인 또는 배지 (무드 색) |
| 페이징 | 무한 스크롤, 20건 단위 |
| 탭 | 카드 → `/photo/:id` |
| 필터 | 상단 칩바 (지도와 상태 공유) |

**카드 위계**: 사진 > 캡션 > 무드 > 태그 > 시각. **캡션이 사진 다음으로 커야 한다** — 이 앱이 파는 게 그거다.

> **날짜별 그룹인 이유**: 역지오코딩이 없어 동네 이름을 모른다(4.3). 다만 날짜 그룹이 이 앱에 더 맞다 — 사람은 "성수동에서 찍은 것들"보다 **"지난 토요일에 뭘 봤지"** 로 기억한다.

**빈 상태**: "아직 기록이 없어요. 첫 산책을 남겨보세요." + 촬영 CTA.

## 11. 대시보드 (`/dashboard`)

**사진에 없던 사실을 알려주는 화면.**

| # | 위젯 | 내용 |
|---|---|---|
| 1 | **요약 스트립** | `총 47장 · 12일 · 태그 23종` |
| 2 | **태그 워드클라우드** | 상위 30개. 빈도 비례 글자 크기. 탭 → 피드 필터 |
| 3 | **요일별 기록 수** | 월~일 7개 막대. 최다 요일 강조 |
| 4 | **무드 분포** | 도넛 8조각 + 범례. 탭 → 피드 필터 |

**구현 규칙**

- **집계는 Postgres에서 한다.** 사진 500장을 클라이언트로 내려받아 세지 않는다
- **요일 계산은 사용자 로컬 타임존 기준.** `at time zone 'Asia/Seoul'` 을 적용해야 한다. UTC로 세면 **밤 9시 이후 사진의 요일이 하루씩 밀린다**
- **워드클라우드는 라이브러리를 쓰지 않는다.** 빈도 → `font-size` 스케일 + flex-wrap 배치로 충분하다. 회전·곡선 배치는 읽기만 어려워진다
- 차트 라이브러리는 경량 1종으로 통일

### 11.1 임계치 처리 (중요)

> 사진 3장으로 만든 도넛 차트는 통찰이 아니라 **소음**이다. "당신의 무드는 100% 고요입니다"는 아무 의미가 없다.

| 사진 수 | 노출 |
|---|---|
| **10장 미만** | 요약 스트립만. 나머지 자리에 `사진 10장이 모이면 분석이 열려요 (지금 4장)` + 진행 바 |
| 10장 이상 | 전체 위젯 |

이 잠금은 **징벌이 아니라 기대감 장치**다. 남은 개수를 정확히 보여준다. 잠금 화면도 **정식 시안으로 그린다** — 대부분의 신규 사용자가 처음 보는 대시보드가 이 화면이다.

## 12. 사진 상세 (`/photo/:id`)

| 순서 | 요소 |
|---|---|
| 1 | 사진 (`view`, 탭하면 `original`) |
| 2 | **캡션** — `caption_user` 우선, 없으면 `caption_ai` |
| 3 | 무드 배지 (탭 → 8종 변경 드롭다운) |
| 4 | 태그 칩 (탭 → 피드 필터) |
| 5 | 메타: 날짜 · 시각 |
| 6 | 한마디 (있으면) |
| 7 | 작은 지도 (핀 1개) — `no_location` 이면 [위치 지정] |
| 8 | 액션: [캡션 수정] [다시 만들기] [원본 다운로드] [삭제] |

- 캡션 수정은 **인라인 편집.** 별도 페이지 이동 없음
- 수정 시 "AI가 쓴 원래 문장 보기"를 접힌 상태로 제공
- 한마디를 나중에 추가하면 **[다시 만들기]가 강조된다** (미반영 상태 알림)
- 다시 만들기는 `caption_ai` 를 덮어쓴다. **`caption_user` 가 있으면 확인을 받는다**

## 13. 설정 (`/settings`) · 화면 상태 목록

### 13.1 설정

계정 정보 / 로그아웃 / **온보딩 다시 보기** / 저장 용량 표시 / 데이터 내보내기 / **계정 삭제**

> 계정 삭제는 v1 필수다. 개인 사진을 서버에 보관하는 서비스가 삭제 경로를 제공하지 않는 건 곤란하다. 삭제 시 Storage 객체까지 완전 제거한다.

### 13.2 전역 상태 (디자인 필요)

| 상태 | 트리거 | UI |
|---|---|---|
| **SPLASH** | PWA 실행 | 아이콘 + 배경색 |
| **ONBOARDING** | 최초 접속 | 3장 캐러셀 + 건너뛰기 |
| **UNAUTH** | 비로그인 | 로그인 화면 |
| **INITIAL** | 로그인 직후, 핀 0 | 지도 + 안내 문장 (8.2) |
| **LOADING** | 핀 조회 중 | **지도는 즉시 렌더**, 핀만 페이드인 |
| **OFFLINE** | 네트워크 없음 | 상단 얇은 배너. **기능을 막지 않는다** |
| **UPLOADING** | 큐 대기분 존재 | 배너 `3장 업로드 중…` |
| **LOCATION_DENIED** | 위치 거부 | 촬영 가능, 지도는 마지막 위치 |
| **DASH_LOCKED** | 사진 10장 미만 | 요약만 + 진행 바 (11.1) |
| **CAPTIONS_OFF** | 캡션 비활성 | "캡션 생성이 일시 중지되었습니다". 저장은 계속 |
| **ERROR** | 조회 실패 | 원인 + [다시 시도] |

배너는 **3종 한 벌**로 디자인한다 (정보 / 진행 / 경고). 높이 동일, 색만 다르게.

---

# PART 3 — 디자인 파운데이션

> **이 파트는 제안이다.** 기존 PROJECT HUB의 라이트 에디토리얼 톤을 기준선으로 삼았다. 방향 전환 시 이 파트만 교체하면 PART 2는 대부분 살아남는다.

## 14. 디자인 원칙

### 14.1 세 가지 원칙

1. **사진이 주인공이다.** UI가 채도를 가지면 사진과 싸운다. UI는 무채색에 가깝게 가고, **유일한 유채색 시스템은 무드 8색**이 가져간다.
2. **빈 화면이 기본값이다.** 첫 사용자는 사진 0장으로 시작한다. 모든 화면의 빈 상태를 **정식 시안으로** 그린다. 빈 상태를 나중에 붙이면 티가 난다.
3. **기다림을 재촉하지 않는다.** 캡션은 백그라운드에서 만들어진다. 스피너·퍼센트·카운트다운을 쓰지 않고, 자리만 잡아두고 조용히 채운다.

### 14.2 무드 팔레트 — 유일한 유채색 시스템

무드는 **도넛 차트 조각 · 지도 핀 테두리 · 피드 카드 라인 · 상세 배지** 네 곳에 쓰인다.

| key | 라벨 | HEX | 정의 (AI 프롬프트에 그대로 전달) |
|---|---|---|---|
| `calm` | 고요 | `#7C8FA0` | 정적이고 인적이 드묾. 시간이 멈춘 듯한 장면 |
| `warm` | 따뜻 | `#E0A356` | 볕과 온기. 사람의 흔적이 남아 있는 장면 |
| `lonely` | 쓸쓸 | `#4A5878` | 비어 있음, 저물어감, 거리감 |
| `lively` | 활기 | `#E2703A` | 움직임과 소란, 색이 많음 |
| `dreamy` | 몽환 | `#A38BC4` | 흐릿함, 안개, 비현실감 |
| `fresh` | 청량 | `#5FB49C` | 맑음, 바람, 초록과 하늘 |
| `cozy` | 아늑 | `#C9A88A` | 좁고 편안한 공간, 실내감 |
| `strange` | 낯섦 | `#8E5B7E` | 어긋남, 기묘함, 처음 보는 것 |

**제약 3가지**

- **8색이 도넛에서 서로 구분되어야 한다.** 인접 조각의 명도 차이를 확보할 것
- **핀 테두리로 썼을 때 사진을 죽이지 않아야 한다.** 채도가 높으면 2px 테두리가 사진보다 눈에 띈다
- **색으로만 구분하지 않는다.** 무드는 반드시 **라벨을 병기**한다 (접근성)

> 위 HEX는 초안이다. 실제 사진 30장에 얹어보고 조정해야 한다. → 24장 열린 이슈 5번

### 14.3 UI 컬러 토큰 (제안)

```css
:root {
  /* Base — 사진을 방해하지 않는 종이 톤 */
  --bg:           #FAF8F4;   /* 페이지 배경 */
  --surface:      #FFFFFF;   /* 카드 · 시트 · 모달 */
  --surface-sunk: #F2EFE8;   /* 스켈레톤 · 비활성 영역 */

  /* Text */
  --ink:          #1C1F1D;   /* 캡션 · 제목 */
  --ink-muted:    #5F625B;   /* 메타 · 시각 · 보조 */
  --ink-faint:    #9A9C94;   /* 플레이스홀더 */

  /* Line */
  --line:         #E6E1D7;
  --line-strong:  #D3CCBE;

  /* Accent — UI 전용. 무드 색과 절대 섞지 않는다 */
  --accent:       #2E4A3C;   /* 딥 그린 — 버튼 · 링크 · 활성 탭 */
  --accent-hover: #223829;
  --accent-soft:  #E7EDE8;   /* 배지 배경 */
  --on-accent:    #FAF8F4;

  /* Status */
  --warn:         #B4762A;
  --error:        #A34133;
  --dim:          rgba(28, 31, 29, 0.45);
}
```

**색 사용 규칙**

- **액센트(딥 그린)는 UI 조작을 위한 색이다.** FAB, 활성 탭, 주요 버튼, 선택된 칩에만 쓴다
- **무드 색은 데이터를 위한 색이다.** 버튼이나 탭에 무드 색을 쓰지 않는다
- 한 화면에서 액센트 면적 10% 이하
- 상태 색을 새로 만들지 않는다. 위 토큰 안에서 해결한다

### 14.4 타이포그래피

| 용도 | 폰트 |
|---|---|
| 본문 · UI · 캡션 (국문) | **Pretendard** |
| 숫자 · 영문 라벨 | Pretendard + `font-variant-numeric: tabular-nums` |

카운터(`핀 47 · 12일`)는 숫자가 바뀔 때 폭이 흔들리면 안 되므로 **tabular-nums 필수**.

| 토큰 | 크기 | 굵기 | 용도 |
|---|---|---|---|
| `onboard-h` | 28 / 34 | 600 | 온보딩 헤드라인 |
| `h1` | 22 | 600 | 화면 타이틀, 대시보드 위젯 제목 |
| `caption` | **18** | 500 | **AI 캡션 — 사진 다음으로 큰 요소** |
| `body` | 15 | 400 | 본문, 안내 문구 |
| `meta` | 13 | 400 | 시각, 정확도, 보조 정보 |
| `label` | 12 | 500 | 태그 칩, 무드 배지, 탭 라벨 |
| `counter` | 15 | 600 | 누적 카운터 (tabular-nums) |

**규칙**

- 굵기는 **400 / 500 / 600 세 가지만**
- 캡션 행간 1.4, 본문 1.6
- **캡션은 1행이 원칙이지만 2행까지 깨지지 않게 설계한다** (30자 + 좁은 화면)

### 14.5 레이아웃 · 간격

| 항목 | 값 |
|---|---|
| 기준 폭 | **375px** (모바일 우선). 320px에서 깨지지 않을 것 |
| 좌우 여백 | 20px |
| 간격 단위 | 4px 배수 (4·8·12·16·20·24·32·48) |
| 탭바 높이 | 56px + safe-area-inset-bottom |
| FAB | 지름 60px, 탭바 중앙에서 위로 12px 돌출 |
| 터치 영역 | 최소 44×44px. **촬영 버튼은 56px 이상** |

| 요소 | 반경 |
|---|---|
| 사진 · 카드 | 12px |
| 시트 상단 | 20px |
| 버튼 | 10px |
| 태그 칩 · 무드 배지 | 999px (pill) |
| 지도 핀 | 원형 |

### 14.6 모션

| 위치 | 사용 | 사양 |
|---|---|---|
| 저장 시트 오르내림 | ✅ | 280ms, `cubic-bezier(0.32, 0.72, 0, 1)` |
| 핀 등장 (촬영 직후) | ✅ | 스케일 0.6 → 1, 240ms. **첫 핀은 조금 더 크게 튀게** |
| `pending` → `ready` 전환 | ✅ | 캡션 페이드인 200ms. **깜빡임 금지** |
| 탭 전환 | ❌ | 즉시 전환 |
| 지도 팬·줌 | 네이티브 | 커스텀 이징 금지 |
| 스켈레톤 | ❌ | **shimmer 애니메이션을 쓰지 않는다.** 정적 회색 바 |

마지막 항목이 중요하다. shimmer는 "로딩 중"을 강조하는 장치인데, 이 앱의 캡션 대기는 **사용자가 신경 쓰면 안 되는 시간**이다.

### 14.7 컴포넌트 목록

| 컴포넌트 | 변형 |
|---|---|
| 지도 핀 | ready(무드 8색) / pending / offline / selected / cluster |
| 태그 칩 | 기본 / 선택 / 대시보드 워드클라우드 |
| 무드 배지 | 8색 × (소형: 피드, 대형: 상세) |
| 사진 카드 | ready / pending / failed |
| 배너 | 정보 / 진행 / 경고 |
| 버튼 | primary / secondary / text / FAB |
| 스켈레톤 | 캡션 1행 / 카드 / 워드클라우드 |
| 빈 상태 | 지도 / 피드 / 대시보드 잠금 |
| 하단 시트 | 저장 / 무드 선택 / 확인 |

---

# PART 4 — 기술 사양

## 15. 시스템 아키텍처

```
[브라우저 / PWA]
   │
   ├─ Supabase Auth (JWT)
   │
   ├─ 사진 업로드 ──────────▶ [Supabase Storage]
   │   (thumb · view · original)   photos/{user_id}/{photo_id}/
   │
   ├─ 메타 INSERT ──────────▶ [Postgres + PostGIS]
   │   (좌표 · 시각 · 한마디)        RLS로 user_id 격리
   │
   ├─ 대시보드 집계 ────────▶ [Postgres RPC]
   │
   └─ POST /api/caption   { photoId }
       Authorization: Bearer <사용자 access token>
              │
              ▼
   [Vercel 서버리스 함수]  ← Gemini 키가 존재하는 유일한 곳
    app/api/caption/route.ts
              │
              ├─ JWT 검증 · 소유권 확인 · 레이트리밋(RPC)
              ├─ Storage 에서 view.webp 다운로드
              │
              └─ Gemini Vision  (프롬프트는 코드 내장 상수)
                   출력: { caption, tags[], mood }
              │
              └─ photos UPDATE (status: ready)
```

> **클라이언트는 Gemini의 존재조차 모른다.** 보내는 것은 자기 access token 과 `photoId` 뿐이다.

| 선택 | 이유 |
|---|---|
| **RLS가 격리를 보장한다** | "각 사용자가 자기 것만 본다"를 애플리케이션이 아니라 **DB 레벨**에서 강제. 프론트에 버그가 나도 남의 사진이 새지 않는다 |
| **런타임이 하나다** | Vercel 배포 시 Route Handler 하나가 그대로 서버리스 함수가 된다. 배포·환경변수·로그가 한 곳에서 끝난다 |
| **PostGIS로 지도 쿼리가 싸진다** | "현재 화면 영역 안의 핀"을 공간 인덱스로. 핀이 수천 개여도 전체를 내려받지 않는다 |
| **집계는 서버에서** | 대시보드를 위해 사진 500장을 클라이언트로 내려받아 세는 건 낭비다 |

## 16. 데이터 모델

```sql
create table profiles (
  id                uuid primary key references auth.users on delete cascade,
  display_name      text,
  onboarded_at      timestamptz,
  caption_calls_1h  int         not null default 0,
  calls_window_at   timestamptz not null default now(),
  created_at        timestamptz not null default now()
);

create type mood_key as enum
  ('calm','warm','lonely','lively','dreamy','fresh','cozy','strange');

create table photos (
  id            uuid primary key default gen_random_uuid(),
  user_id       uuid not null references auth.users on delete cascade,

  storage_path  text not null,
  width         int,
  height        int,

  location      geography(Point, 4326),   -- 촬영 시점 Geolocation (EXIF 아님)
  accuracy_m    numeric,                  -- null = 위치 없음
  captured_at   timestamptz not null,
  source        text not null default 'camera',   -- camera | gallery

  user_note     text,                     -- "한마디" (선택)

  caption_ai    text,                     -- AI 원본. 절대 덮어쓰지 않음
  caption_user  text,                     -- 사용자 수정본
  mood          mood_key,
  mood_edited   boolean not null default false,

  status        text not null default 'pending',
  retry_count   int  not null default 0,
  error_code    text,
  prompt_version text,

  created_at    timestamptz not null default now(),
  updated_at    timestamptz not null default now()
);

create index photos_loc_idx  on photos using gist (location);
create index photos_time_idx on photos (user_id, captured_at desc);
create index photos_mood_idx on photos (user_id, mood);

create table tags (
  id         uuid primary key default gen_random_uuid(),
  user_id    uuid not null references auth.users on delete cascade,
  name       text not null,
  use_count  int  not null default 0,
  unique (user_id, name)
);

create table photo_tags (
  photo_id uuid references photos on delete cascade,
  tag_id   uuid references tags   on delete cascade,
  primary key (photo_id, tag_id)
);
```

**설계 근거**

| 결정 | 이유 |
|---|---|
| `caption_ai` / `caption_user` 분리 | 사용자가 고쳐도 AI 원본이 남는다. **"사람들이 얼마나 고쳤나"가 곧 캡션 품질 지표**가 된다 |
| `mood_edited` 플래그 | 무드 분류 정확도를 재는 유일한 신호. 비율이 높으면 정의나 프롬프트를 고쳐야 한다 |
| `mood` 가 enum | DB가 값을 강제한다. 차트 조각이 8개를 넘을 수 없다 |
| `source` 컬럼 | 갤러리 사진은 위치 신뢰도가 다르다 |
| `accuracy_m` 저장 | 골목 GPS 오차 30~50m. 낮은 정확도 핀을 다르게 표시할 수 있다 |
| 태그가 `user_id` 를 가짐 | 전역 테이블이면 남의 태그가 노출된다 |
| 산책 세션 테이블 없음 | "사진 낱개" 확정. 사진이 유일한 엔티티다 |

### 16.1 RLS

**모든 테이블에 RLS를 켠다. 예외 없다.**

```sql
create policy "own photos" on photos
  for all using (auth.uid() = user_id) with check (auth.uid() = user_id);

create policy "own photo_tags" on photo_tags
  for all using (
    exists (select 1 from photos p
            where p.id = photo_tags.photo_id and p.user_id = auth.uid())
  );
```

> **조인 테이블의 RLS를 빠뜨리는 것이 가장 흔한 유출 경로다.**

### 16.2 Storage

```
photos/                      ← private 버킷 (public 절대 금지)
  {user_id}/{photo_id}/
      thumb.webp    400px    지도 핀 · 피드 카드
      view.webp     1600px   상세 화면 · AI 전송용
      original.jpg           원본 보관
```

- 접근은 **서명 URL**로만, 유효기간 1시간
- **파생본은 업로드 전 클라이언트에서 만든다** (Canvas)
- **AI에는 `view` 를 보낸다.** 원본은 토큰만 늘고 품질은 같다

## 17. AI 처리 (Gemini Vision)

> **프롬프트는 기획 결정 대상이 아니다.** `lib/gemini/prompt.ts` 에 상수로 내장되어 코드와 함께 배포된다. 설정 화면도, 사용자 입력도, 운영자 튜닝 UI도 없다. **`GEMINI_API_KEY` 를 꽂는 것이 유일한 조작이다.**

### 17.1 키가 곧 스위치다

| 키 상태 | 동작 |
|---|---|
| **없음** | `/api/caption` → `503`. **사진은 `pending` 으로 정상 저장되고 지도·피드에 뜬다.** 앱은 멀쩡히 동작 |
| **있음** | 업로드 직후 자동 생성. 사용자가 누를 버튼 없음 |
| **나중에 꽂음** | 그 시점부터 자동 적용 + `resumePendingCaptions()` 가 밀린 사진을 회수 |

이 설계 덕분에 **캡션 없이도 v1을 먼저 띄울 수 있다.** 지도·피드·저장·PWA를 완성해 배포하고 키는 나중에 꽂아도 된다.

### 17.2 입력 재료

| 재료 | 출처 |
|---|---|
| 사진 | `view.webp` (1600px) |
| 촬영 시각 | `captured_at` → `Asia/Seoul` "오후 4시 12분" |
| **사용자 한마디** | `user_note`. 있으면 가장 강한 재료 |
| 기존 태그 상위 30개 | `tags.use_count` desc — 태그 수렴용 |
| 무드 8종 정의 | 14.2 표를 그대로 주입 |

### 17.3 출력 계약

Structured output(`responseSchema`)으로 강제. **`mood` 는 스키마 레벨 enum** 이라 8종 밖 값이 나올 수 없다.

```json
{ "caption": "담장은 오후를 견디는 중이었다", "tags": ["담장","오후빛","골목"], "mood": "calm" }
```

**서버 검증** — 하나라도 어긋나면 저장하지 않는다.

| 필드 | 규칙 |
|---|---|
| `caption` | 비어 있지 않음 · **30자 이내** · 개행 없음 |
| `mood` | 8종 중 하나 |
| `tags` | 1~4개 · 각 6자 이내 · `#` 제거 · 중복 제거 |

> **캡션 없이 사진만 남는 것은 허용하지만, 깨진 데이터가 저장되는 것은 허용하지 않는다.** 무드가 하나만 오염돼도 도넛 차트 전체가 틀린 말을 하게 된다.

### 17.4 품질 규칙 (프롬프트 내장)

**캡션 톤 — 짧은 시, 1행.**

> "담장은 오후를 견디는 중이었다"
> "골목이 잠깐 숨을 멈췄다"
> "빛이 여기서 한 번 접힌다"

- 30자 이내, 은유적. 설명문 금지
- **사진에 없는 사물·사람·동물을 지어내지 않는다** (은유적 톤의 최대 위험)
- 시각·기온·장소명 등 메타데이터를 캡션에 직접 쓰지 않는다. 화면에 이미 있다
- 이모지·해시태그·마침표 금지
- `~하는 듯했다`, `~같았다` 등 AI 클리셰 금지
- 한마디가 있으면 그 정서를 최우선 반영
- **태그는 기존 태그 재사용을 우선** — 태그 폭발 방지
- 무드는 애매해도 지배적 인상 하나를 고른다. **`calm` 기본값 쏠림 방지**

프롬프트 수정 시 `PROMPT_VERSION` 을 올린다. `photos.prompt_version` 에 기록되어 개선 전후를 비교할 수 있다.

## 18. 보안 · 비용 가드레일

### 18.1 정책 방향

> **v1은 사용량 하드 리밋을 걸지 않는다.** 다만 아래 가드레일과 킬 스위치를 **갖춘 상태로** 출시한다.

### 18.2 필수 가드레일 (출시 조건)

| # | 항목 | 사양 |
|---|---|---|
| 1 | **JWT 검증** | Route Handler 첫 단계에서 `auth.getUser()`. 실패 시 401. **이게 없으면 내 엔드포인트는 인터넷 전체의 무료 Vision API가 된다** |
| 2 | **소유권 검증** | service_role은 RLS를 우회하므로 쿼리에 `.eq('user_id', userId)` 를 직접 건다 |
| 3 | **호출 카운트** | `consume_caption_quota` RPC. 60회/시. **서버리스는 상태가 없으므로 반드시 DB에서 센다** |
| 4 | **업로드 상한** | 파일당 25MB, MIME `image/*` 화이트리스트 |
| 5 | **재시도 상한** | `retry_count` 3회. 도달 시 `failed` 확정. 클라이언트 백오프(0/2/6초)와 **둘 다** 둔다 |
| 6 | **킬 스위치** | 환경변수 `CAPTIONS_DISABLED=true`. 값만 바꿔 재배포하면 즉시 멈춘다 |

> 5번이 실무에서 가장 자주 사고를 낸다. 청구서가 밤사이 불어나는 원인은 대개 악의적 사용자가 아니라 **재시도 루프**다.

### 18.3 키 노출 원칙

| 환경변수 | 위치 | 노출 |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | 클라이언트 | **정상** |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | 클라이언트 | **정상.** RLS가 방어선 |
| `SUPABASE_SERVICE_ROLE_KEY` | 서버 전용 | 절대 금지 |
| **`GEMINI_API_KEY`** | 서버 전용 | 노출 시 즉시 과금 사고 |

**구조적 차단 장치**

- `lib/gemini/*` 와 `lib/supabase/admin.ts` 최상단에 **`import 'server-only'`**. 클라이언트에서 실수로 import 하면 **빌드가 실패한다** — 런타임 사고로 번지기 전에 CI에서 잡힌다
- Gemini 키는 쿼리스트링이 아니라 **`x-goog-api-key` 헤더**로 보낸다. URL 로그에 남지 않는다
- `.gitignore` 에 `.env`, `.env.local`, `.env*.local` 필수. 커밋되는 env 파일은 값이 빈 `.env.example` 하나

### 18.4 스토리지 한계

| 항목 | 값 |
|---|---|
| 사진 1장당 | 원본 4MB + view 200KB + thumb 30KB ≈ **4.3MB** |
| Supabase 무료 티어 | 1GB → **약 240장이면 소진** |
| 사용자 10명 × 월 30장 | 월 **1.3GB 증가** |

> **무료 티어로는 실사용 1개월을 못 버틴다.** 출시 전 결론 필요 → 24장 열린 이슈 4번

## 19. 배포 (Vercel 서버리스)

`app/api/caption/route.ts` 는 Vercel에서 **서버리스 함수 하나**로 빌드된다.

| 설정 | 필요 | 없으면 |
|---|---|---|
| `runtime = 'nodejs'` | 필수 | `Buffer` 가 없어 base64 변환이 터진다 |
| `maxDuration = 60` | 필수 | Gemini가 3~8초 걸리는데 기본 타임아웃에 잘린다 |
| `dynamic = 'force-dynamic'` | 필수 | **응답이 캐싱돼 다른 사진에 같은 캡션이 붙는다** |
| 레이트리밋을 DB RPC로 | 필수 | 함수는 매 호출 새로 뜬다. 메모리 카운터는 무의미하다 |

환경변수는 Vercel Dashboard의 Production / Preview / Development에 각각 등록한다. **값을 바꾼 뒤 재배포해야 반영된다.**

> **Preview 환경에 Production과 같은 Supabase 프로젝트를 물리지 않는다.** PR 미리보기가 실서비스 사진을 건드린다.

**과금 사고 시**: ① `CAPTIONS_DISABLED=true` 재배포 → 즉시 정지 ② 키 폐기·재발급 ③ `retry_count` 이상치 확인 ④ 상한 낮춰 재개

## 20. PWA · 오프라인

### 20.1 필수 사양

- `manifest.json` — 아이콘(192/512/maskable) · `display: standalone` · `background_color` · `theme_color`
- Service Worker — 앱 셸 캐싱. **지도 타일은 캐싱하지 않는다** (용량)
- **iOS 스플래시** — Safari는 manifest로 스플래시를 만들어주지 않는다. `apple-touch-startup-image` 를 해상도별로 넣거나 앱 셸 자체를 스플래시처럼 그린다
- **iOS 설치 안내** — Safari에는 설치 프롬프트가 없다. 첫 방문 시 1회 직접 안내

### 20.2 오프라인 촬영 큐

산책 중에는 연결이 끊긴다. **오프라인에서 찍은 사진을 잃으면 이 앱은 신뢰를 잃는다.**

| 항목 | 사양 |
|---|---|
| 저장소 | **IndexedDB** (Blob + 메타). localStorage는 5MB라 불가 |
| 큐 표시 | 상단 배너에 대기 건수 |
| 재시도 | `online` 이벤트 감지 시 순차 업로드 |
| **iOS 제약** | Background Sync 미지원. **앱 재진입 시점 재시도 경로를 반드시 둔다** |
| 저장소 영속성 | `navigator.storage.persist()`. iOS는 7일 미사용 시 삭제 가능 → **진입 시 즉시 업로드 시도** |

> **오프라인 큐는 임시 보관소지 최종 저장소가 아니다.** 브라우저 저장소를 신뢰하지 않는 설계를 하고, 사용자에게도 "업로드 대기 중"임을 계속 보여준다.

## 21. 프로젝트 구조

```
pinwalk/
├─ app/
│  ├─ (onboarding)/ splash · intro       온보딩 3장
│  ├─ (auth)/login/
│  ├─ (main)/
│  │  ├─ page.tsx                        지도 (기본 탭)
│  │  ├─ feed/ · dashboard/
│  │  └─ layout.tsx                      탭바 + 중앙 FAB
│  ├─ capture/ · photo/[id]/ · settings/
│  └─ api/caption/route.ts               ★ Vercel 서버리스 함수
│
├─ lib/
│  ├─ mood.ts                            ★ 무드 8종 단일 진실 공급원
│  ├─ gemini/prompt.ts                   ★ 프롬프트 상수 + 응답 스키마
│  ├─ gemini/generate.ts                 ★ 호출 · 검증 (server-only)
│  ├─ supabase/admin.ts                  ★ service_role (server-only)
│  ├─ caption/trigger.ts                 자동 호출 · pending 회수
│  ├─ image/resize.ts                    ★ Canvas 리사이즈
│  ├─ geo/location.ts                    ★ Geolocation 래퍼
│  ├─ offline/queue.ts                   ★ IndexedDB 큐
│  └─ map/                               Leaflet 래퍼 · MAP_TILE_URL
│
├─ supabase/migrations/
│  ├─ 0001_init.sql                      스키마 + RLS + mood enum
│  ├─ 0002_caption_rpc.sql               consume_caption_quota · attach_tags
│  └─ 0003_stats_rpc.sql                 대시보드 집계 3종
│
├─ .env.example · .gitignore · DEPLOY.md
└─ public/manifest.json
```

★ = **위험이 집중된 파일.** 리뷰를 여기에 집중한다.

**`lib/mood.ts` 는 무드의 단일 진실 공급원이다** — DB enum, 프롬프트, 차트 색, 지도 핀 테두리, 필터 UI가 전부 이 파일을 참조해야 한다. **무드 정의가 두 곳에 존재하는 순간 차트가 거짓말을 시작한다.**

---

# PART 5 — 실행

## 22. 기능 우선순위

### Must (v1 출시 조건)
- [ ] 스플래시 (iOS 대응 포함)
- [ ] 온보딩 3장 · 최초 1회 · 건너뛰기
- [ ] 로그인 / 로그아웃 / 계정 삭제
- [ ] **RLS 전 테이블 적용 (조인 테이블 포함 검증)**
- [ ] 카메라 촬영 → 위치 획득 → 로컬 리사이즈 → 업로드
- [ ] 갤러리 업로드 (위치 확인 플로우)
- [ ] 사진 상태 머신 6종
- [ ] **Vercel 서버리스 캡션 함수** (JWT·소유권·레이트리밋 포함)
- [ ] 업로드 직후 **자동 트리거** + 앱 진입 시 `pending` 회수
- [ ] AI 캡션 · 태그 · **무드 8종 분류**
- [ ] **18.2 가드레일 6개 전부**
- [ ] **환경변수 분리 검증** — 빌드 산출물에 `GEMINI_API_KEY` 문자열이 없는지 확인
- [ ] 지도 탭 (썸네일 핀 · 무드 색 · 클러스터 · 카운터 · bbox 쿼리)
- [ ] 피드 탭 (날짜 그룹 · 무한 스크롤)
- [ ] 대시보드 탭 (워드클라우드 · 요일별 · 무드 분포 · **10장 임계치**)
- [ ] 태그 / 무드 필터 (지도·피드 공유)
- [ ] 사진 상세 · 캡션 수정 · 무드 변경 · 다시 만들기
- [ ] 오프라인 큐
- [ ] PWA 설치 (manifest + SW + iOS 안내)
- [ ] **빈 상태 / 핀 1~3개 구간 설계**

### Should (v1.1)
핀 위치 수동 보정 · 데이터 내보내기 · 캡션 톤 선택 · 태그 병합 · 월별 회고 카드 · 갤러리 다중 업로드

### Could (추후)
동네 이름(역지오코딩 재도입) · 동네 수집률 / 안개 걷히기 · 무드 히트맵 레이어 · 공유 이미지 생성

### Won't (v1)
공유·팔로우 · 경로 추적 · 사진 편집 · 푸시 알림 · 다국어

## 23. 비기능 요구사항

| 항목 | 기준 |
|---|---|
| 성능 | 셔터 → 핀 표시 3초 이내. 지도 첫 렌더 2초 이내 |
| 지도 | 핀 500개에서 팬·줌 프레임 드랍 없음 (클러스터링 전제) |
| 캡션 지연 | 저장 → `ready` p90 **5초 이내** |
| 대시보드 | 집계 응답 1초 이내 (사진 1,000장 기준) |
| 반응형 | **모바일 우선.** 320px~ 정상. 데스크톱은 중앙 정렬 |
| 브라우저 | iOS Safari 16+, Android Chrome 최신 2버전 |
| 접근성 | 터치 영역 44px 이상(촬영 56px). **무드를 색으로만 구분하지 않는다 — 라벨 병기** |
| 보안 | 전 테이블 RLS · private 버킷 · 서명 URL 1시간 |
| 프라이버시 | 위치·사진 용도를 온보딩에서 고지. 계정 삭제 시 Storage 포함 완전 삭제 |

## 24. 리스크

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| 1 | **스토리지 비용** | 무료 티어 240장 소진 | 18.4. 출시 전 결론 |
| 2 | **은유적 캡션의 환각** | 사진에 없는 것을 지어냄 | 17.4 규칙 + 출시 전 50장 수동 검수 |
| 3 | **무드 분류 편향** | 특정 무드 쏠림으로 도넛이 무의미 | `mood_edited` 비율 모니터링. 30% 초과 시 정의 재작성 |
| 4 | **대시보드 빈 구간** | 초기 사용자에게 무의미한 차트 | 11.1 10장 임계치 |
| 5 | **핀 3개 구간의 초라함** | 2회차 촬영 미발생 | 8.3 별도 설계 |
| 6 | **iOS PWA 저장소 삭제** | 오프라인 큐 유실 | 20.2 진입 시 즉시 업로드 |
| 7 | **태그 폭발** | 워드클라우드가 안개가 됨 | 17.4 재사용 프롬프트. 3개월 뒤 재평가 |
| 8 | **OSM 타일 정책** | 트래픽 증가 시 차단 | `MAP_TILE_URL` 상수화 |
| 9 | **요일 집계 타임존 오류** | 밤 사진의 요일이 밀림 | 11장 명시. **테스트 케이스 필수** |
| 10 | **캡션 재시도 루프** | 과금 사고 | 18.2-5 상한 |
| 11 | **`NEXT_PUBLIC_` 오타** | Gemini 키 전면 노출 | `server-only` 로 빌드 실패 유도 |
| 12 | **Route Handler 캐싱** | 다른 사진에 같은 캡션 | `force-dynamic` 필수 |
| 13 | **Vercel 함수 타임아웃** | 캡션이 매번 잘림 | `maxDuration = 60` + 플랜 상한 확인 |

## 25. 열린 이슈

**디자인 착수 전 필요**

1. **서비스명** — `PINWALK` 은 임시. 기존 네이밍 패턴(PROJECT HUB / NEWS FINDER / REVIEW LENS / AirChips / Weather Globe)과 어울리는 후보 확정. **온보딩·스플래시·로고에 직결된다**
2. **디자인 방향 확정** — PART 3은 라이트 에디토리얼 기준 제안이다. 사진 중심 앱이라 다크 UI가 사진을 더 살린다는 반론이 가능하다
3. **무드 8종 확정** — 라벨·정의·HEX를 실제 사진 30장에 얹어보고 조정. **6종으로 줄이는 것도 검토** (도넛 가독성)

**개발 착수 전 필요**

4. **원본 보관 정책** — 18.4 수치 확인 후 유료 전환 / 원본 미보관 / 90일 후 삭제 중 택일
5. **동네 이름을 정말 포기하는가** — 피드 그룹·검색·"다닌 동네 수"가 전부 여기 걸려 있다. Nominatim(OSM, 키 불필요)은 이미 OSM 지도를 쓰는 이상 연동을 새로 늘리는 게 아니라 **쓰던 걸 한 겹 더 쓰는 것**에 가깝다
6. **갤러리 다중 업로드** — v1 1장 제한이 답답할 수 있다. 큐 폭주 위험과 저울질
7. **Gemini 모델 ID** — 착수 시점 최신 Flash 계열 확인. 환경변수로 덮어쓸 수 있어 배포 후에도 조정 가능
8. **소셜 로그인 1종** — Google / Kakao / Apple 중
9. **OSM 타일 제공처** — 공용 서버 / 무료 티어 서비스 중

---

## 부록 A. API 계약

```
POST /api/caption
  Headers: Authorization: Bearer <supabase access_token>
  Body:    { "photoId": "uuid" }

  200  { status, caption, tags[], mood }
  400  BAD_REQUEST
  401  UNAUTHORIZED
  404  NOT_FOUND            (없거나 남의 사진)
  409  RETRY_EXHAUSTED      (retry_count >= 3)
  429  RATE_LIMITED
  502  { code, retryable }  (Gemini 실패)
  503  CAPTIONS_DISABLED    (키 없음 또는 킬 스위치)
```

**클라이언트 재시도**: 백오프 `0s / 2s / 6s`, 최대 3회.
`401 / 403 / 404 / 409 / 429 / 503` 은 **재시도하지 않는다** — 특히 429를 재시도하면 레이트리밋을 더 세게 때린다.

## 부록 B. 환경변수

| 변수 | 필수 | 설명 |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | ✓ | 클라이언트 노출 정상 |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | ✓ | 클라이언트 노출 정상 |
| `GEMINI_API_KEY` | — | **없으면 캡션 자동 비활성.** 앱은 정상 동작 |
| `SUPABASE_SERVICE_ROLE_KEY` | ✓ | 서버 전용 |
| `GEMINI_MODEL` | — | 모델 ID 덮어쓰기 |
| `CAPTION_RATE_LIMIT_PER_HOUR` | — | 기본 60 |
| `CAPTIONS_DISABLED` | — | `true` 면 강제 정지 |

---

*이 문서는 v0.1~v0.3 인터뷰 결과를 통합한 v1.0이다.*
*열린 이슈 1~3번이 해소되면 디자인이 확정 단계로 넘어간다.*
